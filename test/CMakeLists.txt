# Root cmake for all the test sub programs for the
# hpm library.

# Add hpmglbunny test.
ADD_SUBDIRECTORY(glbunny)
# Add hpm assert for testing each function and assert them.
ADD_SUBDIRECTORY(hpmassert)
